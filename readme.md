Gui python app to invert a film negative or image's colors (batch coming soon)

Prerequisites:
pip install pillow
pip install numpy
pip install matplotlib.pyplot

credits: m1sho, libraries: pillow, numpy, pyplot, tkinter

